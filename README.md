# RunBeat

把任意一首歌改成**指定步频**的跑步音乐 —— 速度跟着脚步走，音高不变。

做法是整曲时间伸缩（time-stretch），不重排拍点、不掺合成器，只把原曲的时间轴拉长或压短到你想要的密度。

---

## 能做什么

1. **上传**：mp3 / wav / m4a / aac / flac / ogg / opus / wma，单文件 ≤ 20 MB、时长 ≤ 10 分钟
2. **检测原曲 BPM**：自动检测，附可信度分档
   - 检测常把节拍**翻倍或减半**，所以给出一组候选值（±2x、2/3x、3/2x）一键切换
   - 候选都不对可以直接手填 —— 填错的话后面一切都会跟着错
3. **核对拍点**：示波器上叠画波形、检测到的拍点线、播放位置
   - 点「听节拍对齐」会播**原速原曲 + 按拍点打出的 click**，不掺任何变速，用来确认检测准不准
4. **裁剪区间**：在整曲缩略波形上拖两个手柄选一段，试听和生成的都只处理这一段
   - 起止点松手时**自动对准最近的鼓点**（最多挪半拍），这样变速后开头正好压在一拍上
   - 也可以直接在两个时间框里填 `1:20`，不选就是整首
   - 「试听这段」当场听这一小段，**不用先跑一遍第 3、4 节**；调完起止点自动把播放位置摆好：
     改起点就从新起点听，改终点就跳到终点前 5 秒专门听收尾
5. **设定目标步频**：120~220 spm，1:1（每步踩一拍）与 1:2（每两步踩一拍）两种换算方式
6. **限制变速幅度**：自己设 `min~max` 倍率，超出范围的会被截到边界并回显实际步频
7. **试听**：浏览器实时变速、零延迟，可叠加节拍声，改步频时正在播的音频自动跟上
8. **生成下载**：服务端完整渲染（音质比试听好），可叠加节拍声，成品下载后自行导入手机音乐 App

---

## 快速开始

需要 **64 位 Python 3.10+**（librosa 依赖 64 位，32 位装不上）。

```bash
# 1. 创建虚拟环境
python -m venv .venv

# 2. 安装依赖（官方源慢可换镜像，见下方说明）
.venv/Scripts/python.exe -m pip install --isolated \
  -i https://mirrors.aliyun.com/pypi/simple/ \
  -r requirements.txt
```

Windows 下双击 `run.bat`，或手动启动：

```bash
.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

然后打开 <http://127.0.0.1:8000>。开发时用 `run-dev.bat`（改代码自动重载）。

**关于 ffmpeg**：项目通过 `imageio-ffmpeg` 自带 ffmpeg 二进制，**不需要你手动安装 ffmpeg**。
如果你想用自己那份，设置环境变量 `RUNBEAT_FFMPEG` 指向可执行文件即可。

> 装依赖时如果遇到 `Could not find a version that satisfies the requirement ... (from versions: none)`，
> 通常是代理或镜像的问题：先 `unset http_proxy https_proxy`，再把 `-i` 换成阿里云或官方源
> （清华镜像偶发返回空版本列表）。

---

## 界面流程

页面从上到下五节，按顺序走一遍就是完整流程：

| 节 | 名字 | 做什么 |
|---|---|---|
| 1 | 原曲节奏 | 看波形和拍点、切换候选 BPM、手填修正、「听节拍对齐」校对 |
| 2 | 裁剪区间 | 拖手柄选一段（不选就是整首），点「试听这段」当场听这一小段 |
| 3 | 目标步频 | 拖步频、选换算方式、设变速范围，下方状态条给自然度提示 |
| 4 | 试听 | 实时变速试听，可叠加节拍声，进度条可拖到任意位置 |
| 5 | 生成下载 | 服务端完整渲染这一段并下载 |

第 4 节的试听是**浏览器实时变速**，好处是零延迟、拖步频能立刻听到；代价是走浏览器的变速算法，音色和成品略有出入。第 5 节才是服务端用 ffmpeg 精确渲染的那份，**音质更好**。

---

## 核心逻辑

### 倍率是怎么算出来的

```
1:1  →  需要的音乐 BPM = 目标步频
1:2  →  需要的音乐 BPM = 目标步频 ÷ 2
倍率 = 需要的音乐 BPM ÷ 原曲 BPM
倍率 = clamp(倍率, 用户设的 min_ratio, 用户设的 max_ratio)
实际步频 = 原曲 BPM × 倍率 × (1:2 时为 2)
```

举例：原曲 120 BPM，目标 170 步频，按 1:1 需要 1.417x（+41.7%）。
若用户设的范围是 0.85x~1.15x，则实际按 1.15x 生成，最终是 138 步频。

这个换算前后端各有一份实现，**必须保持一致**：

- 后端 `app/audio.py: plan_ratio()`
- 前端 `static/app.js: computePlan()`

### 变速为什么不变调

用 ffmpeg 的 `atempo` 滤镜做时间伸缩，只改速度不改音高。
`atempo` 单级只支持 0.5~2.0 倍，超出范围时 `audio.py` 会自动拆成多级串联。

### 裁剪区间

选区从上传后就一直带着走，影响三件事：试听范围、听节拍对齐范围、导出范围。

- **起点吸附**：松手时把起止点各挪到最近的拍点上（最多半拍）。始终吸附而不是"距离太远就不吸"，
  否则吸附半径小于半个拍距时会出现"有时候吸、有时候不吸"的薛定谔行为。
- **最短 5 秒**：再短的话变速之后听不出节奏对不对，后端会拒绝并告诉你还差多少。
- **拍点按原曲时间轴算**：裁掉前后不影响中间的对齐，所以选区内部的 click 位置和整曲时完全一致。

#### 「试听这段」听哪一段

裁剪卡片上的「试听这段」按钮和第 4 节共用同一套播放链路 —— 变速倍率、节拍声开关、播到选区末尾自动停，
全部自动一致，不存在"两处试听行为不一样"的问题。同一个按钮兼作停止。

关键是**改了起止点之后从哪儿接着听**，规则是：

| 改动 | 下次试听的锚点 |
|---|---|
| 起点 | 新的起点 |
| 终点 | **终点前 5 秒**（`CLIP_TAIL_SEC`） |
| 区间本身短于 5 秒 | 自动退回区间开头 |

改终点专门听尾巴，是因为收尾才是最容易出问题的地方：变速之后最后一拍有没有被切掉、和下一段接得顺不顺，
只有听结尾那几秒才知道；从起点重听一整段反而听不出差异。

这条规则用一段锚点函数统一实现（`clipAnchorTime()`），两个入口都走到它：

- 松手拖手柄 / 交回输入框 → `repositionAfterClip(anchor)` 把进度条和播放位置都挪到锚点
- 正在试听时 → 直接改 `currentTime` 跳过去继续放，**不打断播放**；没在放 → 只摆好位置等按钮

按钮旁边那行小字（`clipTip`）会把锚点写成「试听将从 02:35 开始（终点前 5 秒）」——
"改终点会自动跳到终点前 5 秒"这件事光看进度条看不出来，得说出来。

### 节拍声：试听一条路，导出另一条路

两边都要发声，但实现完全不同：

| | 试听（浏览器） | 导出（服务端） |
|---|---|---|
| 实现 | Web Audio 振荡器实时合成 | `build_click_track()` 生成 wav，再用 ffmpeg `amix` 混进去 |
| 音色 | 必须复刻服务端的包络（`exp(-70t)`，50 ms 自然衰减） | 权威模板 |
| 音量 | `metroGain` 直接作为峰值振幅 | `volume={metronome_gain}` |

两边共用的常量在 `config.py`（`METRONOME_FREQ` / `METRONOME_GAIN` 等），前端 `app.js` 顶部有对应副本 —— **改一处必须同步改另一处**。

两个容易踩的坑：

- **别给 click 额外乘 0.5**。试听和导出的峰值振幅规则必须一致，早期版本多乘了一次 0.5，听感直接差一倍。
- **包络必须逐点复刻**。用 `exponentialRampToValueAtTime` 近似 4 ms 快衰减的写法，能量只有服务端模板的 47%（-8.1 dB），会"看起来在响、实际听不见"。现在用 `setValueCurveAtTime` 按服务端模板逐点采样。

导出时还会把原曲压到 `METRONOME_BED_GAIN = 0.82` 给 click 让头空间，最后 `alimiter` 兜底防削波；
但**导出成品不压原曲**（`METRONOME_EXPORT_BED_GAIN = 1.0`）—— 跑步听的主体还是音乐，不该因为多了一层 click 就整体变小。

### 试听：音乐和节拍声必须走同一张音频图

这是试听里最容易出错的一环，值得单独说。

`<audio>` 元素有自己的输出通路，Web Audio 有自己的输出通路。两条路各自缓冲，浏览器**不保证采样级同步**（典型几十毫秒）。如果音乐走 `<audio>`、click 走 Web Audio，位置算得再准，最后一段路也会错开 —— 表现是"一直差那么一点点，但不会越听越歪"。

所以勾上节拍声后会调 `attachMusicToGraph()`，用 `createMediaElementSource()` 把音乐也接进同一张 AudioContext 图，两条声音共用一套时钟和输出缓冲。不勾节拍声就不接（零风险，保持原生播放路径）。

> `createMediaElementSource()` 对同一个元素**只能调用一次**，再调会抛错，所以有 `musicAttached` 标记守着。
> 另外音乐改走音频图后，一旦 AudioContext 被系统挂起音乐就哑了，`keepMusicAlive()` 负责抢救。

**排程方式**：不做"每批重新读一次播放位置"的定位 —— 那样读数的几毫秒抖动会**整批烙进**这一批 click，批与批之间互相错开（实测间隔误差中位 3.7 ms、最大 9.6 ms，听感就是"越往后越乱"）。
现在改成持续采样 `(ctx时刻, 媒体位置)`，滚动最小二乘拟合一条直线 `mediaT = a·ctxT + b`，每一拍该响的时刻直接从这条线上查：

- 所有 click 共用同一条平滑的线，批间不再跳变
- 斜率 `a` 是**实测**的变速倍率，浏览器实际变速与标称值的细微出入（实测约 87 ppm）被顺带吸收，不再随时间累积

几个关键参数（都在 `app.js` 顶部，**别随手改回去**）：

| 常量 | 值 | 作用 |
|---|---|---|
| `CLICK_LOOKAHEAD` | 2.0 s | 提前排程窗口。原来只有 0.25 s，主线程一卡（画波形、切后台被降频到 1 次/秒）就会整声丢失 |
| `CLICK_STALE_SEC` | 0.15 s | 过期判定。宁可略晚一点响，也不要整声消失 |
| `CLICK_SETTLE_MS` | 250 ms | 开播/拖进度条后等媒体时钟稳下来再排程，修掉开播第一声早响约 100 ms |
| `CLICK_TICK_MS` | 40 ms | 巡检间隔，由自校正计时器驱动，不累积漂移 |
| `BEAT_FIT_WINDOW` | 8 s | 拟合窗口，太短压不住抖动、太长跟不上变化 |
| `BEAT_FIT_MIN` | 25 | 样本数下限（约 1 秒），不够时退回标称倍率换算，只用于开局过渡 |

音量用**共用的输出增益节点**而不是每声 click 各带一个 —— 提前量放大到 2 秒后，逐声自带增益会让拖音量滑块要等 2 秒才生效。

### 听节拍对齐

第 1 节的校对工具：把原曲压到 `0.82`、叠上按检测拍点打出的 click，混成一段（默认 15 秒）让你用耳朵确认。
它播的是**原速原曲**，不掺任何变速，所以是判断"检测准不准"最直接的手段：

- 这里就对不上 → 是检测错了（多半是半速/倍速误判，改 BPM 数字或换候选值）
- 这里对得上、导出的成品对不上 → 检测没问题，问题在别处

---

## 目录结构

```
runbeats/
├── app/
│   ├── config.py     配置与限制（含所有可调常量）
│   ├── audio.py      ffmpeg 封装、BPM 检测、倍率换算、时间伸缩、click 生成
│   ├── store.py      文件登记表、预览缓存、过期清理
│   └── main.py       FastAPI 接口
├── static/
│   ├── index.html    页面结构（五节）
│   ├── style.css
│   └── app.js        全部前端逻辑：波形、拍点、裁剪、试听排程、导出
├── scripts/
│   ├── selftest.py        核心算法自检
│   ├── check_frontend.py  前端静态校验
│   └── e2e_test.py        端到端 HTTP 测试
├── docs/             界面截图
├── data/             运行时生成，见下节
├── requirements.txt
├── run.bat
└── run-dev.bat
```

---

## data 目录是干什么的

**全部是运行时产物，删掉不影响程序运行** —— 服务重启后会按需重建，只是已上传的文件和已生成的成品会丢失（内存登记表本来就是易失的）。

| 子目录 | 装什么 | 什么时候产生 | 会自动清理吗 |
|---|---|---|---|
| `uploads/` | 你上传的**原始音频** | 每次上传 | ✅ 6 小时后（仍登记在册的不删） |
| `analysis/` | 从原曲转出的**单声道 22 kHz wav**，只用于 BPM 检测和画波形 | 每次上传 | ✅ 6 小时后 |
| `previews/` | 服务端渲染的**试听片段缓存**（按 `文件ID+位置+倍率+节拍音量` 做哈希键） | 请求片段时 | ✅ 6 小时后 |
| `outputs/` | **完整成品**，供页面内试听和下载 | 每次生成 | ✅ 6 小时后 |
| `tmp/` | 混音用的中间文件 | 叠加节拍声时 | ✅ 用完即删 |

自动清理由 `store.purge_expired()` 负责，服务启动后每 30 分钟跑一次，保留期 `RETENTION_HOURS = 6.0`。

**关于 `uploads/` 的一个历史坑（已修复）**：
`previews` / `outputs` / `analysis` / `tmp` 一直是按 mtime 直接扫目录删除的，但 `uploads/` 曾经**只**通过内存登记表 `_files` 清理。
登记表是易失的 —— 服务一重启（改后端代码、换目录、调试）内存表就清空，**重启前上传的文件全部失去登记，变成永远不会被自动删除的孤儿**。
实测长期下来堆到了 850 MB / 89 个孤儿文件。

现在 `purge_expired()` 把 `UPLOAD_DIR` 也纳入了 mtime 扫描，同时加了一道保护：
**仍登记在册的上传文件不按 mtime 删**（它们可能正被当前会话使用），只交给按 `created_at` 的那一段统一收尾。
所以"上传后可安全清理"和"不会误删正在用的文件"两件事都成立。

怀疑磁盘上有孤儿时：`GET /api/health` 返回的 `store.tracked_files` 是当前内存表的条数，
和磁盘上 `uploads/` 的实际文件数一比，差额就是孤儿。

**想手动清理时**：直接删 `data/` 下的文件是安全的（都是临时产物），但要注意两点 ——
正在使用中的文件被删会导致页面上那首歌失效；删除不可逆。按 mtime 挑出超过保留期的文件删是最稳妥的做法。

---

## 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 自检：ffmpeg 路径、输出格式、内存登记数与各目录文件数 |
| POST | `/api/upload` | 上传音频，返回时长、检测 BPM、候选 BPM、波形峰值 |
| GET | `/api/waveform/{file_id}` | 整曲波形峰值（上传时已算好，秒回） |
| GET | `/api/beats/{file_id}?bpm=` | 按指定 BPM 给出拍点时间（原曲时间轴，秒） |
| POST | `/api/metronome` | 「听节拍对齐」：原曲片段 + click 混音 |
| POST | `/api/preview` | 渲染一段变速试听片段（带缓存）。**前端已不再调用**，保留给自动化测试 |
| POST | `/api/export` | 渲染成品，支持 `start` / `length` 指定裁剪区间 |
| GET | `/api/download/{name}` | 下载成品 |

静态资源：`/media/source`、`/media/preview`、`/media/output` 分别映射三个数据目录，根路径映射 `static/`。

`/api/export` 返回的字段里，`clipped` 表示这次是否带了裁剪区间，文件名会带上 `_0m20s-0m26s` 这样的区间标记，
避免同一首歌的不同片段互相覆盖。

---

## 自检与测试

三个脚本，都不依赖外部测试框架：

```bash
# 核心算法自检：ffmpeg 能力、BPM 检测精度、倍率换算、变速后时长与 BPM 复检、片段截取、click 对齐
.venv\Scripts\python.exe scripts\selftest.py

# 前端静态校验：JS 引用的 id 是否都存在、标签闭合、CSS 花括号配对
.venv\Scripts\python.exe scripts\check_frontend.py

# 端到端 HTTP 测试（需先启动服务）：上传 -> 波形 -> 拍点 -> 预览缓存 -> 裁剪导出 -> 下载 -> 异常输入
.venv\Scripts\python.exe scripts\e2e_test.py
```

`selftest.py` 会自己合成固定节拍的打击音轨做验证，不依赖任何外部音频素材。
`e2e_test.py` 里有一项用**频率标记音频**（每 8 秒换一个主频）裁三段导出，再用频谱测成品主频 ——
用来证明"裁的确实是那一段"，而不只是生成了一个长度正确的文件。

---

## 已知精度

| 项目 | 结果 |
|---|---|
| BPM 检测（合成 120 BPM） | 读到 **120.2**，误差 0.17% |
| 变速时长 | 误差 < 0.1%（24.00s 按 ×1.0815 → 22.19s，理论值 22.19s） |
| 导出成品里的 click 落点 | 中位误差 **0 ms**，不累积漂移 |
| 试听里相邻 click 的间隔误差 | 中位 **0.26 ms**、后段最大 0.61 ms（改前 3.7 ms / 9.6 ms，且越往后越差） |
| 试听里 click 的落点误差 | p90 约 10 ms，最大 11 ms（改前 108 ms） |
| 裁剪试听的锚点 | 与理论值一致：改起点落在新起点、改终点落在「终点 − 5 秒」，且不打断正在播放的音频 |
| 主线程卡顿（8 × 350 ms）下丢声 | **0**（改前丢 2 声） |

BPM 检测精度靠 `ANALYSIS_HOP = 256` 撑着 —— 这是把帧移从 librosa 默认的 512 降下来换的。
默认值下帧率只有 43 Hz，同一条 120 BPM 音频会被读成 117.45（误差 2.1%）。**别改回 512。**

关于检测误差，有两类要分清：

- **量化误差**（良性，±1.2~1.6%）：自相关只能在整数帧上比较，BPM 只能落在网格点上。
  hop=256 时 120 BPM 附近是 `123.05 / 120.19 / 117.45`，间距约 2.9 BPM。
  也就是说一首标准 128 BPM 的歌只会读成 129.2 或 126.1，**永远读不到 128**。对跑步影响 ±3 spm，可接受。
- **倍速 / 半速误判**（恶性，误差 100%）：自相关对 T、2T、4T 都有峰，挑错就整体差一倍。
  比量化误差大 60 多倍 —— **可靠性问题几乎全部来自这里**，用第 1 节的「听节拍对齐」或候选值切换就能识别。

另外注意：**不要用 1:1 / 1:2 开关去修检测错误**。半速误判时切 1:2 恰好能抵消（看着像修好了），
但倍速误判时切 1:2 只会错更远。开关不是修错用的，检测错了就改数字。

检测结果按曲风的可靠程度：电子 / 流行 / 摇滚可信；民谣 / 慢歌 / 爵士易半速倍速误判；
古典 / 现场版 / 纯人声 / 环境音乐**本来就没有唯一 BPM**（速度一直在变）。

---

## 注意事项

- 所有文件只在本机处理，不上传任何第三方服务。
- 变速幅度越大越难听。经验值：**±15% 内基本无感，±25% 开始别扭，超过 30% 基本不能听**。
  页面上的状态条会按绿 / 黄 / 红给出提示。
- 第 4 节的试听是浏览器实时变速，音色与成品不完全一致；要听真实效果请用第 5 节。
- 请仅处理你有权使用的音频。公开运营需自行考虑版权合规问题。
